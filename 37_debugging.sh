#!/bin/bash
#set -e is used to teminate the script as soon as the script fails
#set -x is used to debugg line by line so that if something faild it should be easy to track.

set -x
set -e
pwd
hostname
cd /home/enterpva
echo " Hello world"
